Create a database named as testdb and run mosquitto on port 1881.

Register atleast 9 users and then run node_pub && Sub then sensor and finnaly gateway.
Add as much as node/sensor/data you want to add.

I had use necessary details in message instead of topic, We can use a unique topic to avoid someone
who trying to sending data by guessing topic
